NODE_ENV=development

POSTGRES_DB=DEV-ProjectAdmin
POSTGRES_USER=admin
POSTGRES_HOST=localhost
POSTGRES_PORT=6500
POSTGRES_PASSWORD=postgres
DATABASE_URL=`postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?pgbouncer=true`
DIRECT_URL=`postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}`

NEXT_PUBLIC_APP_URL="http://localhost:3000"

# NextAuth.js
AUTH_SECRET=supersecretkeythatishouldchange
